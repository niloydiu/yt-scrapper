Place your logo image here as `logo.png` so the web app can display it in the header.

If you attached an image in the chat, download it and save it at:

  static/logo.png

Accepted formats: PNG, JPG, SVG. Recommended size: 256x256 (or a square) with a transparent background.

After adding the file, restart the Flask app if it's already running.
